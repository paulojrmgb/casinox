# CasinoX v1.1 — correção de imagens

A v1.1 troca o background-image dos cards por `<img>` real, com cache-busting 1.1. Isso evita que o navegador renderize cards pretos quando a imagem não é aplicada ao background.
